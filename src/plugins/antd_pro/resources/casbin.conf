# Request definition
[request_definition]
r = perm

# Policy definition
[policy_definition]
p = perms

# Policy effect
[policy_effect]
e = some(where (p.eft == allow))

# Matchers
[matchers]
m = r.perm in p.perms